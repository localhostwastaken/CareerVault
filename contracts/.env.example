# Only needed to deploy to Polygon Amoy testnet (server BLOCKCHAIN_DRIVER=amoy).
POLYGON_RPC_URL=
ANCHOR_PRIVATE_KEY=

# Only needed to verify the deployed contract on PolygonScan (npm run verify:amoy).
ETHERSCAN_API_KEY=
